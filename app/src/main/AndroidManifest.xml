<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".oilActivity"></activity>
        <activity android:name=".smokeActivity" />
        <activity android:name=".vegetableActivity" />
        <activity android:name=".vitaminActivity" />
        <activity android:name=".waterActivity" />
        <activity android:name=".sleepActivity" />
        <activity android:name=".fattyFish" />
        <activity android:name=".coffeActivity" />
        <activity android:name=".junkactivity" />
        <activity android:name=".nutactivity" />
        <activity android:name=".sugaractivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".splash"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>